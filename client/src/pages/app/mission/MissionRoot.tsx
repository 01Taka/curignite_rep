import React from 'react';
import { Routes, Route } from 'react-router-dom';

interface MissionRootProps { }

const MissionRoot: React.FC<MissionRootProps> = () => {
  return (
    <Routes>
      {/* <Route path='/*' element={</>} /> */}
    </Routes>
  );
};

export default MissionRoot;